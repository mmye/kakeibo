# デザインテーマ仕様書

## コンセプト

**「家庭の温もり × 財務の明晰さ」**

家族で家計を見つめ直すためのダッシュボード。柔らかく親しみやすい雰囲気を保ちながら、データは明確で鋭く読み取れるデザイン。

### デザイン原則

1. **Warmth（温かみ）** - 家庭的で安心感のある色調とフォルム
2. **Clarity（明晰さ）** - 数字とデータが一目で理解できる視認性
3. **Balance（調和）** - 情報過多にならない適切な余白と階層構造
4. **Trust（信頼感）** - 落ち着いた配色で長期利用に耐えるデザイン

---

## カラーパレット

### プライマリカラー（メインアクセント）

| 名称 | HEX | RGB | 用途 |
|------|-----|-----|------|
| Primary | `#2D6A4F` | rgb(45, 106, 79) | ヘッダー、主要ボタン、リンク |
| Primary Light | `#40916C` | rgb(64, 145, 108) | ホバー状態、サブ要素 |
| Primary Dark | `#1B4332` | rgb(27, 67, 50) | アクティブ状態、強調 |

> 深いフォレストグリーン。成長・安定・繁栄を象徴し、家計の健全さを表現。

### セカンダリカラー（補助）

| 名称 | HEX | RGB | 用途 |
|------|-----|-----|------|
| Secondary | `#B07D62` | rgb(176, 125, 98) | サブナビ、アイコン、装飾 |
| Secondary Light | `#D4A574` | rgb(212, 165, 116) | 背景アクセント |
| Secondary Dark | `#8B5E3C` | rgb(139, 94, 60) | 強調装飾 |

> ウォームテラコッタ。木や土を連想させ、家庭の温もりを演出。

### セマンティックカラー（意味を持つ色）

| 名称 | HEX | 用途 |
|------|-----|------|
| Income（収入） | `#059669` | 収入金額、プラス表示、ポジティブ指標 |
| Income Light | `#D1FAE5` | 収入の背景色 |
| Expense（支出） | `#DC2626` | 支出金額、マイナス表示、警告 |
| Expense Light | `#FEE2E2` | 支出の背景色 |
| Neutral（収支ゼロ） | `#6B7280` | ニュートラル値 |
| Warning | `#D97706` | 予算超過の警告 |
| Warning Light | `#FEF3C7` | 警告の背景色 |

### ベースカラー（背景・テキスト）

| 名称 | HEX | 用途 |
|------|-----|------|
| Background | `#FDFBF7` | ページ背景（温かみのあるオフホワイト） |
| Surface | `#FFFFFF` | カード・パネル背景 |
| Surface Hover | `#F9F7F3` | カードホバー時 |
| Border | `#E5E1D8` | ボーダー、区切り線 |
| Border Strong | `#D1CCC0` | 強調ボーダー |

### テキストカラー

| 名称 | HEX | 用途 |
|------|-----|------|
| Text Primary | `#1F2937` | 本文、見出し |
| Text Secondary | `#6B7280` | 補足テキスト、ラベル |
| Text Tertiary | `#9CA3AF` | プレースホルダー、無効状態 |
| Text On Primary | `#FFFFFF` | プライマリ背景上のテキスト |

---

## タイポグラフィ

### フォントファミリー

```css
/* 見出し・数字（鋭さを表現） */
--font-heading: "Noto Sans JP", "Hiragino Kaku Gothic ProN", sans-serif;

/* 本文（読みやすさ重視） */
--font-body: "Noto Sans JP", "Hiragino Kaku Gothic ProN", sans-serif;

/* 数値・金額（等幅で揃える） */
--font-mono: "JetBrains Mono", "SF Mono", "Consolas", monospace;
```

### フォントサイズ

| 名称 | サイズ | 行高 | 用途 |
|------|--------|------|------|
| Display | 32px / 2rem | 1.2 | ダッシュボードタイトル |
| H1 | 24px / 1.5rem | 1.3 | セクション見出し |
| H2 | 20px / 1.25rem | 1.4 | カードタイトル |
| H3 | 16px / 1rem | 1.4 | サブセクション |
| Body | 14px / 0.875rem | 1.6 | 本文 |
| Small | 12px / 0.75rem | 1.5 | 補足、キャプション |
| Tiny | 10px / 0.625rem | 1.4 | ラベル、バッジ |

### 金額表示

| 状態 | サイズ | ウェイト | 備考 |
|------|--------|----------|------|
| 大金額（サマリー） | 28px | 700 (Bold) | 等幅フォント使用 |
| 中金額（カード内） | 20px | 600 (SemiBold) | 等幅フォント使用 |
| 小金額（テーブル） | 14px | 500 (Medium) | 等幅フォント使用 |

```css
/* 金額表示の基本スタイル */
/* Tailwindクラス: font-mono tabular-nums tracking-tight */

/* Tailwind拡張（tailwind.config.ts で定義） */
/* text-income: #059669 */
/* text-expense: #DC2626 */
```

**Tailwindでの使用例**:
```tsx
<span className="font-mono tabular-nums text-income">+¥1,234</span>
<span className="font-mono tabular-nums text-expense">-¥5,678</span>
```

---

## スペーシング

### 基本単位

基本単位: **4px**

| 名称 | 値 | 用途 |
|------|-----|------|
| space-1 | 4px | 最小間隔、インラインアイコン |
| space-2 | 8px | 要素内の小間隔 |
| space-3 | 12px | ラベルとフィールド間 |
| space-4 | 16px | カード内パディング |
| space-5 | 20px | セクション間 |
| space-6 | 24px | カード間 |
| space-8 | 32px | 大セクション間 |
| space-10 | 40px | ページ余白 |
| space-12 | 48px | セクション区切り |

### コンテナ幅

| 名称 | 最大幅 | 用途 |
|------|--------|------|
| Container SM | 640px | モーダル、フォーム |
| Container MD | 768px | サイドパネル |
| Container LG | 1024px | メインコンテンツ |
| Container XL | 1280px | ダッシュボード全体 |
| Container 2XL | 1536px | ワイドスクリーン |

---

## ボーダー・角丸

### 角丸（Border Radius）

| 名称 | 値 | 用途 |
|------|-----|------|
| radius-sm | 4px | 小ボタン、バッジ |
| radius-md | 8px | ボタン、入力フィールド |
| radius-lg | 12px | カード、パネル |
| radius-xl | 16px | モーダル、大きなカード |
| radius-full | 9999px | 丸ボタン、アバター |

> 角丸は柔らかさを出しつつ、やや控えめに。過度な丸みは避ける。

### ボーダー

```css
/* 標準ボーダー */
--border-default: 1px solid #E5E1D8;

/* 強調ボーダー */
--border-strong: 1px solid #D1CCC0;

/* フォーカスリング */
--border-focus: 2px solid #2D6A4F;
```

---

## シャドウ

| 名称 | 値 | 用途 |
|------|-----|------|
| shadow-sm | `0 1px 2px rgba(0,0,0,0.05)` | ボタン、バッジ |
| shadow-md | `0 4px 6px rgba(0,0,0,0.07)` | カード（デフォルト） |
| shadow-lg | `0 10px 15px rgba(0,0,0,0.1)` | ホバー時のカード、ドロップダウン |
| shadow-xl | `0 20px 25px rgba(0,0,0,0.1)` | モーダル、オーバーレイ |

> シャドウは控えめに。浮きすぎない自然な影。

---

## コンポーネント仕様

### カード

```
┌─────────────────────────────────────┐
│  [アイコン] タイトル                  │ ← H2, Text Primary
│                                     │
│  ¥483,127                           │ ← 大金額, Bold
│  前月比 +5.2%                        │ ← Small, Income色
│                                     │
└─────────────────────────────────────┘

- 背景: Surface (#FFFFFF)
- 角丸: radius-lg (12px)
- シャドウ: shadow-md
- パディング: space-6 (24px)
- ボーダー: なし（シャドウで浮かせる）
- ホバー: シャドウをshadow-lgに、背景をSurface Hoverに
```

### サマリーカード（3カラム）

```
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│   収入       │ │   支出       │ │  収支        │
│  +483,127円  │ │ -320,450円   │ │ +162,677円   │
│  ▲ 5.2%     │ │  ▼ 12.3%    │ │  ▲ 2.1%     │
└─────────────┘ └─────────────┘ └─────────────┘

- 収入カード左ボーダー: 4px solid Income色
- 支出カード左ボーダー: 4px solid Expense色
- 収支カード左ボーダー: 4px solid Primary色
```

### ボタン

**プライマリボタン**
```css
.btn-primary {
  background: #2D6A4F;
  color: #FFFFFF;
  padding: 10px 20px;
  border-radius: 8px;
  font-weight: 600;
  font-size: 14px;
  box-shadow: 0 1px 2px rgba(0,0,0,0.05);
  transition: all 0.15s ease;
}

.btn-primary:hover {
  background: #40916C;
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
}
```

**セカンダリボタン**
```css
.btn-secondary {
  background: transparent;
  color: #2D6A4F;
  border: 1px solid #2D6A4F;
  padding: 10px 20px;
  border-radius: 8px;
  font-weight: 600;
}

.btn-secondary:hover {
  background: rgba(45, 106, 79, 0.1);
}
```

### テーブル

```
┌──────────┬────────────────────┬───────────┬──────────┐
│ 日付      │ 内容                │ カテゴリ   │ 金額      │ ← ヘッダー
├──────────┼────────────────────┼───────────┼──────────┤
│ 12/25    │ 給料                │ 収入/給与  │ +483,127 │ ← 収入行
│ 12/25    │ マルエツ鹿島田店     │ 食費/食料品│  -3,251  │ ← 支出行
│ 12/24    │ JR東日本モバイルSuica│ 交通費/電車│    -750  │
└──────────┴────────────────────┴───────────┴──────────┘

- ヘッダー背景: #F9F7F3
- ヘッダーテキスト: Text Secondary, 12px, 600
- 行の高さ: 48px
- ホバー行: Background色
- 金額列: 右寄せ、等幅フォント
- ゼブラストライプ: 偶数行 #FDFBF7
```

### チャート

**共通設定**
```css
/* チャートコンテナ */
.chart-container {
  background: #FFFFFF;
  border-radius: 12px;
  padding: 24px;
  box-shadow: 0 4px 6px rgba(0,0,0,0.07);
}

/* チャートタイトル */
.chart-title {
  font-size: 16px;
  font-weight: 600;
  color: #1F2937;
  margin-bottom: 16px;
}
```

**チャートカラー**
```javascript
// カテゴリ別の配色（円グラフ・棒グラフ用）
const CHART_COLORS = {
  食費: '#F59E0B',      // アンバー
  日用品: '#8B5CF6',    // パープル
  交通費: '#3B82F6',    // ブルー
  通信費: '#06B6D4',    // シアン
  教養教育: '#10B981',  // エメラルド
  健康医療: '#EC4899',  // ピンク
  衣服美容: '#F97316',  // オレンジ
  趣味娯楽: '#6366F1',  // インディゴ
  水道光熱費: '#14B8A6', // ティール
  交際費: '#EF4444',    // レッド
  その他: '#6B7280',    // グレー
};

// 収支推移グラフ
const TREND_COLORS = {
  income: '#059669',   // 収入線
  expense: '#DC2626',  // 支出線
  balance: '#2D6A4F',  // 収支線
};
```

### フィルターパネル

```
┌─────────────────────────────────────────────────────┐
│  期間: [2025年] [12月 ▼]   カテゴリ: [すべて ▼]      │
│                                                     │
│  金融機関: [すべて ▼]      検索: [____________ 🔍]  │
└─────────────────────────────────────────────────────┘

- 背景: Surface
- パディング: space-4
- 各入力間隔: space-4
- ドロップダウン: radius-md, border-default
```

---

## レスポンシブブレークポイント

| 名称 | 幅 | レイアウト |
|------|-----|-----------|
| Mobile | < 640px | 1カラム、カード縦積み |
| Tablet | 640px - 1024px | 2カラムグリッド |
| Desktop | 1024px - 1280px | 3カラムグリッド |
| Wide | > 1280px | 4カラムグリッド、サイドバー表示 |

### グリッドレイアウト

```css
/* ダッシュボードグリッド */
.dashboard-grid {
  display: grid;
  gap: 24px;
  padding: 24px;
}

/* Mobile */
@media (max-width: 639px) {
  .dashboard-grid {
    grid-template-columns: 1fr;
    padding: 16px;
    gap: 16px;
  }
}

/* Tablet */
@media (min-width: 640px) and (max-width: 1023px) {
  .dashboard-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* Desktop */
@media (min-width: 1024px) {
  .dashboard-grid {
    grid-template-columns: repeat(3, 1fr);
  }

  .chart-full-width {
    grid-column: span 2;
  }
}
```

---

## アイコン

### 推奨アイコンセット

**Lucide Icons** または **Heroicons** を使用

### 主要アイコン一覧

| 用途 | アイコン名 | 備考 |
|------|-----------|------|
| 収入 | `arrow-up-circle` | Income色で表示 |
| 支出 | `arrow-down-circle` | Expense色で表示 |
| 食費 | `utensils` | |
| 日用品 | `shopping-bag` | |
| 交通費 | `train` | |
| 通信費 | `smartphone` | |
| 健康・医療 | `heart-pulse` | |
| 趣味・娯楽 | `gamepad-2` | |
| フィルター | `filter` | |
| 検索 | `search` | |
| カレンダー | `calendar` | |
| ダウンロード | `download` | |
| 設定 | `settings` | |

### アイコンサイズ

| サイズ | 値 | 用途 |
|--------|-----|------|
| SM | 16px | インラインアイコン、テーブル内 |
| MD | 20px | ボタン内、ラベル横 |
| LG | 24px | カードヘッダー |
| XL | 32px | 空状態、大きな装飾 |

---

## インタラクション

### トランジション

```css
/* 基本トランジション */
--transition-fast: 0.1s ease;
--transition-normal: 0.15s ease;
--transition-slow: 0.3s ease;

/* 使用例 */
.card {
  transition: box-shadow var(--transition-normal),
              transform var(--transition-normal);
}

.card:hover {
  box-shadow: var(--shadow-lg);
  transform: translateY(-2px);
}
```

### フォーカス状態

```css
/* フォーカスリング */
*:focus-visible {
  outline: 2px solid #2D6A4F;
  outline-offset: 2px;
}

/* 入力フィールドのフォーカス */
input:focus {
  border-color: #2D6A4F;
  box-shadow: 0 0 0 3px rgba(45, 106, 79, 0.1);
}
```

---

## CSS変数まとめ

```css
:root {
  /* Colors - Primary */
  --color-primary: #2D6A4F;
  --color-primary-light: #40916C;
  --color-primary-dark: #1B4332;

  /* Colors - Secondary */
  --color-secondary: #B07D62;
  --color-secondary-light: #D4A574;
  --color-secondary-dark: #8B5E3C;

  /* Colors - Semantic */
  --color-income: #059669;
  --color-income-light: #D1FAE5;
  --color-expense: #DC2626;
  --color-expense-light: #FEE2E2;
  --color-warning: #D97706;
  --color-warning-light: #FEF3C7;

  /* Colors - Base */
  --color-background: #FDFBF7;
  --color-surface: #FFFFFF;
  --color-surface-hover: #F9F7F3;
  --color-border: #E5E1D8;
  --color-border-strong: #D1CCC0;

  /* Colors - Text */
  --color-text-primary: #1F2937;
  --color-text-secondary: #6B7280;
  --color-text-tertiary: #9CA3AF;

  /* Typography */
  --font-heading: "Noto Sans JP", sans-serif;
  --font-body: "Noto Sans JP", sans-serif;
  --font-mono: "JetBrains Mono", monospace;

  /* Spacing */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;

  /* Border Radius */
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --radius-xl: 16px;

  /* Shadows */
  --shadow-sm: 0 1px 2px rgba(0,0,0,0.05);
  --shadow-md: 0 4px 6px rgba(0,0,0,0.07);
  --shadow-lg: 0 10px 15px rgba(0,0,0,0.1);

  /* Transitions */
  --transition-fast: 0.1s ease;
  --transition-normal: 0.15s ease;
  --transition-slow: 0.3s ease;
}
```

---

## 実装チェックリスト

- [ ] CSS変数をグローバルに定義
- [ ] フォント（Noto Sans JP, JetBrains Mono）の読み込み
- [ ] Lucide Icons の導入
- [ ] 基本コンポーネント（カード、ボタン、テーブル）のスタイル実装
- [ ] レスポンシブグリッドの実装
- [ ] チャートカラーの適用
- [ ] フォーカス状態・ホバー状態の実装
- [ ] ダークモード対応（将来拡張）
